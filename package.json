{
  "name": "demos",
  "private": true,
  "version": "0.0.0",
  "description": "Anthony's collection of Salesforce demos. Deployables live in apps/; scratch work lives in experiments/.",
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
